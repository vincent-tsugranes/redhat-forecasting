# Template for container registry credentials
# Replace with your actual credentials before applying

apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  namespace: weather-dashboard-dev
  annotations:
    tekton.dev/docker-0: quay.io
type: kubernetes.io/basic-auth
stringData:
  username: YOUR_REGISTRY_USERNAME
  password: YOUR_REGISTRY_PASSWORD

---
# Alternative: Using Docker config format
# apiVersion: v1
# kind: Secret
# metadata:
#   name: registry-credentials
#   namespace: weather-dashboard-dev
# type: kubernetes.io/dockerconfigjson
# stringData:
#   .dockerconfigjson: |
#     {
#       "auths": {
#         "quay.io": {
#           "username": "YOUR_USERNAME",
#           "password": "YOUR_PASSWORD",
#           "email": "YOUR_EMAIL",
#           "auth": "BASE64_ENCODED_USERNAME:PASSWORD"
#         }
#       }
#     }

---
# To create the secret from command line:
# kubectl create secret docker-registry registry-credentials \
#   --docker-server=quay.io \
#   --docker-username=YOUR_USERNAME \
#   --docker-password=YOUR_PASSWORD \
#   --docker-email=YOUR_EMAIL \
#   -n weather-dashboard-dev
