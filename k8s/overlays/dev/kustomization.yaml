apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: weather-dashboard-dev

bases:
  - ../../base

commonLabels:
  environment: dev

patchesStrategicMerge:
  - backend-patch.yaml
  - frontend-patch.yaml

images:
  - name: quay.io/vtsugran/weather-backend
    newTag: "latest"
  - name: quay.io/vtsugran/weather-frontend
    newTag: "latest"
