apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  labels:
    app: postgres
  annotations:
    description: "Database credentials - use SealedSecrets or external-secrets-operator in production"
type: Opaque
stringData:
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
