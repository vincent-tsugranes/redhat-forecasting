apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: frontend
  app.kubernetes.io/component: ui
  app.kubernetes.io/part-of: weather-dashboard

images:
  - name: nginx
    newTag: "1.25-alpine"
