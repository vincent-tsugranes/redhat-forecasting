apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  labels:
    app: backend
  annotations:
    description: "Sensitive values - use SealedSecrets or external-secrets-operator in production"
type: Opaque
stringData:
  QUARKUS_DATASOURCE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  OPENWEATHER_API_KEY: ""
